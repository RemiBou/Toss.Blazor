@if ((Errors?.ContainsKey(FieldName)).GetValueOrDefault(false))
{
    <span class="small form-text text-danger">@Errors[FieldName]</span>

}
@functions{
    [Parameter]
    public string FieldName { get; set; }
    [Parameter]
    public Dictionary<string, string> Errors { get; set; }
}
